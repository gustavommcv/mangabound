# Shared compositions

Feature screens are assembled from shared compositions rather than one-off layouts. The first consuming milestone will introduce `PageShell`, `SettingsSection`, `JobRow`, `IssueCallout`, and `EmptyState` with their real props, accessible behavior, stories, and tests. They are intentionally not speculative placeholder components in the foundation commit.
